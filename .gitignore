# real student submissions - never commit these to a shared repo
submissions/*
!submissions/.gitkeep

# this week's official test cases / rubric - course material, never commit these either
tests/*
!tests/.gitkeep

build_tmp/
results/*.csv
lib/*.jar
__pycache__/
*.pyc
